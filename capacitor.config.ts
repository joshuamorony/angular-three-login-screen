import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-three-login-screen',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
